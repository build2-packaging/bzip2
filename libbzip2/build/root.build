using c

h{*}: extension = h
c{*}: extension = c

# The test target for cross-testing (running tests under Wine, etc).
#
test.target = $c.target

if($c.target.class == 'windows')
{
    info "Only the static library liba{bzip2} is supported on Windows."
    bin.lib = static
}
